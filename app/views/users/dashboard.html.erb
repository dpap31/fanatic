<!DOCTYPE html>
<html>
<head>
	<title><%=@user.name%>'s Fanatic Dashboard</title>
	<%= stylesheet_link_tag "dashboard" %>
</head>
<div class="row clearfix">
	<div class="col-md-8 column">
		<div id="title">
			<a href="/posts"><strong><i class="glyphicon glyphicon-star-empty"></i>  All Posts</strong></a> 
		</div> 

		<div id="new_button">
			<%if can? :create, Post %>
			<a href="/posts/new"><strong><i class="glyphicon glyphicon-plus-sign"></i>  New Post</strong></a>
			<% end %>
		</div>  
		<br>  
		<hr>
		<div id='user_teams'>
			<%if @teams.first%>
			<div class='teamname'>
				<h4><%= link_to "#{@teams.first.location} #{@teams.first.name}",tag_path(@teams.first.name)%></h4>
			</div>
			<table>
				<tr>
					<% hottness(Post.tagged_with(@teams.first.name)).take(3).each do |post| %>
					<div class="pin">
						<div class="image">
							<a href="/posts/<%=post.slug%>"><img class= "img-responsive" src="<%= post.image %>" /></a>
						</div>

						<div class="caption">
							<h4>
								<%= post.title %>
							</h4>
							<div class = 'post-info'>
								<div class = 'tags'>
									<ul class="token-input-token-facebook">
										<li class="token-input-token-facebook"><%= raw post.tag_list.map { |t| link_to t, tag_path(t) }.join('<li class="token-input-token-facebook"> ') %></li></ul>
									</div>
									<div id = 'cheers'>
										<%= pluralize post.reputation_for(:votes).to_i, "cheer" %> 
									</div>
								</div>
							</div>
						</div>
					</td>
				</tr>
				<% end %>
				<% end %>
			</table>
			<%if @teams.second%>
			<div class='teamname'>
				<h4><i class="glyphicon glyphicon-link"></i> <%= link_to "#{@teams.second.location} #{@teams.second.name}", tag_path(@teams.second.name)%></h4>
			</div>
			<table>
				<tr>
					<% hottness(Post.tagged_with(@teams.second.name)).take(3).each do |post| %>
					<div class="pin">
						<div class="image">
							<a href="/posts/<%=post.slug%>"><img class= "img-responsive" src="<%= post.image %>" /></a>
						</div>

						<div class="caption">
							<h4>
								<%= post.title %>
							</h4>
							<div class = 'post-info'>
								<div class = 'tags'>
									<ul class="token-input-token-facebook">
										<li class="token-input-token-facebook"><%= raw post.tag_list.map { |t| link_to t, tag_path(t) }.join('<li class="token-input-token-facebook"> ') %></li></ul>
									</div>
									<div id = 'cheers'>
										<%= pluralize post.reputation_for(:votes).to_i, "cheer" %> 
									</div>
								</div>
							</div>
						</div>
					</td>
				</tr>
				<% end %>
				<% end %>
			</table>
			<%if @teams.third%>
			<div class='teamname'>
				<h4> <i class="glyphicon glyphicon-link"></i> <%= link_to "#{@teams.third.location} #{@teams.third.name}", tag_path(@teams.third.name)%></h4>
			</div>
			<table>
				<tr>
					<% hottness(Post.tagged_with(@teams.third.name)).take(3).each do |post| %>
					<div class="pin">
						<div class="image">
							<a href="/posts/<%=post.slug%>"><img class= "img-responsive" src="<%= post.image %>" /></a>
						</div>

						<div class="caption">
							<h4>
								<%= post.title %>
							</h4>
							<div class = 'post-info'>
								<div class = 'tags'>
									<ul class="token-input-token-facebook">
										<li class="token-input-token-facebook"><%= raw post.tag_list.map { |t| link_to t, tag_path(t) }.join('<li class="token-input-token-facebook"> ') %></li></ul>
									</div>
									<div id = 'cheers'>
										<%= pluralize post.reputation_for(:votes).to_i, "cheer" %> 
									</div>
								</div>
							</div>
						</div>
					</td>
				</tr>
				<% end %>
				<% end %>
			</table>
			<%if @teams.fourth%>
			<div class='teamname'>
				<h4><i class="glyphicon glyphicon-link"></i> <%= link_to "#{@teams.fourth.location} #{@teams.fourth.name}", tag_path(@teams.fourth.name)%></h4>
			</div>
			<table>
				<tr>
					<% hottness(Post.tagged_with(@teams.fourth.name)).take(3).each do |post| %>
					<div class="pin">
						<div class="image">
							<a href="/posts/<%=post.slug%>"><img class= "img-responsive" src="<%= post.image %>" /></a>
						</div>

						<div class="caption">
							<h4>
								<%= post.title %>
							</h4>
							<div class = 'post-info'>
								<div class = 'tags'>
									<ul class="token-input-token-facebook">
										<li class="token-input-token-facebook"><%= raw post.tag_list.map { |t| link_to t, tag_path(t) }.join('<li class="token-input-token-facebook"> ') %></li></ul>
									</div>
									<div id = 'cheers'>
										<%= pluralize post.reputation_for(:votes).to_i, "cheer" %> 
									</div>
								</div>
							</div>
						</div>
					</td>
				</tr>
				<% end %>
				<% end %>
			</table>
			<%if @teams.fifth%>
			<div class='teamname'>
				<h4><i class="glyphicon glyphicon-link"></i> <%= link_to "#{@teams.fifth.location} #{@teams.fifth.name}", tag_path(@teams.fifth.name)%></h4>
			</div>
			<table>
				<tr>
					<% hottness(Post.tagged_with(@teams.fifth.name)).take(3).each do |post| %>
					<div class="pin">
						<div class="image">
							<a href="/posts/<%=post.slug%>"><img class= "img-responsive" src="<%= post.image %>" /></a>
						</div>

						<div class="caption">
							<h4>
								<%= post.title %>
							</h4>
							<div class = 'post-info'>
								<div class = 'tags'>
									<ul class="token-input-token-facebook">
										<li class="token-input-token-facebook"><%= raw post.tag_list.map { |t| link_to t, tag_path(t) }.join('<li class="token-input-token-facebook"> ') %></li></ul>
									</div>
									<div id = 'cheers'>
										<%= pluralize post.reputation_for(:votes).to_i, "cheer" %> 
									</div>
								</div>
							</div>
						</div>
					</td>
				</tr>
				<% end %>
				<% end %>
			</table>
			<%if Post.all.where(:user_id => current_user.id)%>
			<h2> <i class="glyphicon glyphicon-pencil"></i> <%= link_to "My Posts", user_posts_path(@current_user)%></h2>
			<hr>
			<table>
				<% @my_posts.each_slice(3) do |slice| %>
				<tr>
					<% slice.each do |post| %>
					<td>
						<div class="pin">
							<div class="image">
								<a href="/posts/<%=post.slug%>"><img class= "img-responsive" src="<%= post.image %>" /></a>
							</div>

							<div class="caption">
								<h4>
									<%= post.title %>
								</h4>
								<div class = 'post-info'>
									<div class = 'tags'>
										<ul class="token-input-token-facebook">
											<li class="token-input-token-facebook"><%= raw post.tag_list.map { |t| link_to t, tag_path(t) }.join('<li class="token-input-token-facebook"> ') %></li></ul>
										</div>
										<div id = 'cheers'>
											<%= pluralize post.reputation_for(:votes).to_i, "cheer" %> 
										</div>
									</div>
								</div>
							</div>
						</td>
						<% end %>
					</tr>
					<% end %>
				</table>
		</div>
		<%else%>
		<h2>My Posts</h2>
		<%= link_to "Create Post", new_post_path, :class => 'btn btn-primary btn-block'%>
		<% end %>
	</div>
	<div class="col-md-3 column pull-left">
		<div id = 'user_widget'>
		<div id="DashboardProfileCard"> </div>
		<%= image_tag(@user.image, class: 'profile_pic') %>
		
		<div id='first_name'><strong><%= @user.first_name %></strong></div>
		<div id='username'> <em><%= @user.username %></em></div>
		</div>
	<div id='stats_widget'>
		<h4>
			Stats
		</h4>
		<hr>
		<dl class="dl-horizontal">
			<dt>
				Post Count
			</dt>
			<dd>
				<%= @my_posts_count %>
			</dd>
			<dt>
				Cheers
			</dt>
			<dd>
				<%= @my_posts_cheers %>
			</dd>
			<dt>
				Following
			</dt>	
			<dd>
				<%= @user.friendships.count%>
			</dd>
			<div class = 'following_images'>
				<ul id="grid">
					<% for friendship in @user.friendships.take(8) %>
					<li><%= image_tag(friendship.friend.image, size: '40x40') %></li>
					<% end %>
				</ul>
			</dl>
			</div>
			<div id= 'team_widget'>
			<div id= 'team_widget_header'><h4>Teams</h4>
				<hr></div>
				<div class = 'team_images'>
					<% @teams.each do |team| %>
					<%= link_to tag_path(team.name) do%>
						<%= image_tag("logos/#{team.id}.gif", size: "80x55")%>
						<% end %>
						<% end %>
					</div>
				</div>

				<div id= 'activity_widget'>
				<div id= 'activity_widget_header'><h4>Activity</h4>
				<hr></div>
				<dl>
					<dd>
						<%= render template: 'layouts/_activities.html.erb'%>
					</dd>
				</dl>
		</div>
	</div>
	</html>








